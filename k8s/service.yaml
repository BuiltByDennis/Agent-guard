apiVersion: v1
kind: Service
metadata:
  name: agent-proxy-backend
  labels:
    app: agent-proxy
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: agent-proxy
